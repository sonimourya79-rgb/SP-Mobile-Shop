# Leave unset for local dev (uses Vite's proxy to http://localhost:5050, see vite.config.js).
# In production (Vercel), set this to your deployed backend's /api URL, e.g.
# VITE_API_URL=https://sp-mobile-api.onrender.com/api
VITE_API_URL=
